# lg1 = cairo.LinearGradient(0.0, 0.0, 500.0, 500.0)
            # count = 1
            # i = 0.1
            # while i < 1.0:
            #     if count % 2:
            #         lg1.add_color_stop_rgba(i, *FrameColors.FRAME_COLOR_1)
            #     else:
            #         lg1.add_color_stop_rgba(i, *FrameColors.FRAME_COLOR_2)
            #     i = i + 0.1
            #     count = count + 1
            # self.context.set_line_width(2)
            # self.context.rectangle(self.x, self.y, self.width, self.height)
            # self.context.set_source(lg1)
            # self.context.stroke()